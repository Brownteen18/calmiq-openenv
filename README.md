# CalmIQ OpenEnv

AI mental wellness simulation environment for training agents.

## Features
- OpenEnv API (reset, step, state)
- Multi-task evaluation
- Reward shaping
- Personality + random events
- Adaptive behavior memory

## Run

uvicorn app:app --port 7860